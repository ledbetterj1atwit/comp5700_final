predicates: FFree(x) BFree(x) LFree(x) RFree(x) Captured(x) Kinged(x)
constants: WA WB WC WD BA BB BC BD
5 actions

MoveFL piece
  pre:  FFree(piece) LFree(piece)
  preneg:
  del: FFree(piece) LFree(piece)
  add: RFree(piece) BFree(piece)

MoveFR piece
  pre:  FFree(piece) RFree(piece)
  preneg:
  del: FFree(piece) RFree(piece)
  add: LFree(piece) BFree(piece)

MoveBL piece
  pre:  BFree(piece) LFree(piece) Kinged(piece)
  preneg:
  del: BFree(piece) LFree(piece)
  add: RFree(piece) FFree(piece)

MoveBR piece
  pre:  BFree(piece) RFree(piece) Kinged(piece)
  preneg:
  del: BFree(piece) RFree(piece)
  add: LFree(piece) FFree(piece)

King piece
  pre:
  preneg: FFree(piece)
  del:
  add: Kinged(piece)

#CaptureBA_FL piece
#  pre: [Free Sequence that gives position]
#  preneg: Captured(B)
#  del: [Frees befitting new position]
#  add: [Frees Befitting new position] Captured(B)